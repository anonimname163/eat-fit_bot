# Eat&fit — статус (коротко)

- **Рефактор завершён.** Стек: NestJS+TypeORM (`apps/api`) + Next.js (`apps/web`) + `@eatfit/shared`. Монорепо. RU/UZ.
- Git `main`: чисто, синхронизировано с origin. Коммитим/пушим как обычно.
- Локально: `docker compose up -d` → `npm run api:dev` (health :3000). После теста **гасить dev-серверы**.
- Подробная карта кода/архив рефактора: `REFACTOR-PROGRESS.md`, `_bmad-output/`.

## Текущая задача — детальная страница блюда (по референсу eatfit_client_app_live.html)
Референс = только РАСКЛАДКА (не цвета). Добавляем ПОЛНЫЙ набор новых полей блюда: вес/граммовка, дедлайн заказа (HH:MM), состав (ингредиент+граммы, RU/UZ), аллергены (содержит/может содержать, RU/UZ), КБЖУ (ккал/Б/Ж/У).
- Сайт (Mini App): список → тап по блюду → отдельный детальный экран (фото, имя RU/UZ, описание, цена+вес, дедлайн, аккордеоны состав/аллергены/КБЖУ, кнопка заказа снизу).
- Бот: под счётчиком кнопка «Подробнее» → web_app, открывает деталь блюда в Mini App (deep link на id). Описание в боте НЕ показываем.
- Затрагивает: libs/shared (типы), apps/api (entity+DTO+menu service/repo+admin CRUD), apps/web (детальный экран + админ-форма + i18n + навигация), apps/api/telegram (кнопка «Подробнее»).
- Прогресс: [x] модель/типы [x] API (entity+DTO+service+normalize) [x] админ-форма (вес/дедлайн/состав/аллергены/КБЖУ) [x] веб-деталь (DishDetail + навигация + deep-link ?dish) [x] бот-кнопка «Подробнее» (web_app → Mini App)
- Доп. правка: в посте и предпросмотре админки сначала UZ-название, потом RU.
- Сборка: shared + api + web — все зелёные. НЕ закоммичено (жду подтверждения/визуальной проверки). Локально не запускал (риск 409 с прод-ботом).
- ВАЖНО при деплое: новые колонки nullable, synchronize=true (TYPEORM_SYNC) добавит их сам. Если на Railway TYPEORM_SYNC=false — нужна миграция.

## Последнее (2026-06-07…09) — фича МЕНЮ, завершена
Кластер правок меню: блюда по дням недели (витрина показывает только сегодняшние), фото с сайта видно в боте, публикация в канал из веб-админки, в посте/боте/превью — только название+цена, описание только в Mini App. Проверено: согласовано на всех поверхностях, дерево чистое, незавершёнки нет.
Последний коммит: `00268fa feat(menu): … только название и цена; описание в Mini App`.

## Незакрытые мелочи (не срочно)
- `main.ts` TODO(AR-11): нормальный SPA-CSP (nonce/hash) на отдаче статики — отложено с рефактора.
- `eatfit_client_app_live.html` — старый прототип от 27 мая, неотслеживаемый; решить: удалить или оставить.
